# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Full-resolution case-study photography, kept locally as the source for the
# aligned crops in public/images/cases (see tools/align-cases.py). Not shipped.
case-originals

# Alignment strips written by `tools/align-cases.py --check`. Look at them,
# don't commit them.
tools/.check

# Full-resolution hero photography, kept locally as the source for the
# right-sized variants in public/images/hero. Not shipped.
hero-originals
